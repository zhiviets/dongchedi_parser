# Зависимости для weekly_scraper.py (headless-обход в GitHub Actions).
# Отдельный файл от requirements.txt: там ещё и пакеты для сборки
# desktop-приложения (PyInstaller, Eel, pywin32-ctypes и т.д.), которые
# на linux-раннере CI не нужны и часть из них там не ставится.
playwright==1.56.0
beautifulsoup4==4.14.2
requests==2.32.5
pillow==12.0.0
